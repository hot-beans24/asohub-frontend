import { css } from '@emotion/react'

const styles = {
  container: css`
    width: 100%;
    height: 100%;
    display: grid;
    grid-template: 1fr / 1fr;
    grid-template-areas: 'posts';
  `,
}

export default styles
