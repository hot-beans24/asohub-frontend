import { css } from '@emotion/react'

const styles = {
  errorMessage: css`
    color: var(--error-color);
    font-size: 1.4rem;
    font-weight: 500;
  `,
  errorIcon: css`
    margin-right: 8px;
  `,
}

export default styles
