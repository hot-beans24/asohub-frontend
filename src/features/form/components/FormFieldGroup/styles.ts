import { css } from '@emotion/react'

const styles = {
  formFieldGroup: css`
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 40px;
  `
}

export default styles
