import { css } from '@emotion/react'

const styles = {
  formText: css`
    margin-top: 40px;
    color: var(--dark-gray);
    font-size: 1.4rem;
    text-align: center;
  `,
}

export default styles
