import { css } from '@emotion/react'

const styles = {
  formWrapper: css`
    max-width: 300px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 40px;
    position: relative;
  `
}

export default styles
