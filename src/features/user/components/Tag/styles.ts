import { css } from '@emotion/react'

const styles = {
  tag: css`
    height: 34px;
    padding: 0 14px;
    border-radius: 6px;
    background-color: #f5f6f7;
    white-space: nowrap;
    color: var(--dark-gray);
    display: flex;
    justify-content: center;
    align-items: center;
  `,
}

export default styles
